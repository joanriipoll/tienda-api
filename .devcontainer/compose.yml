services:
  workspace:
    build:
      context: ..
      dockerfile: .devcontainer/Dockerfile
    # Keep alive the container in background
    command: sleep infinity
    volumes:
      - ..:/workspace:cached
      - /var/run/docker.sock:/var/run/docker.sock
    extra_hosts:
      - "host.docker.internal:host-gateway"
      - "localhost:host-gateway"